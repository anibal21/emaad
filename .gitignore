# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
*.code-workspace

# Legacy path (unused; prefer projects/)
sessions/
*.session.md
.emaad-cache/

# Ema local workspace config (language preference, etc.)
.emaad/config.json

# User design packages (local workspace — not part of EMAAD product)
projects/*/
projects/index.json

# Secrets — never commit
.env
.env.*
!.env.example
**/credentials*
**/secrets*

# Python / tooling (if used for validators later)
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node (if used for doc generators)
node_modules/
dist/
.turbo/
