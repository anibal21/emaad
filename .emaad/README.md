# Ema workspace config

- `config.json` — your preferences (local; gitignored)
- `config.example.json` — template

Copy the example to `config.json` or let Ema create it on first run when you pick a language.
